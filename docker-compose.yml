version: '3.9'

services:
  complaint-handler:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      - LANGCHAIN_TRACING_V2=true
      - LANGCHAIN_PROJECT=complaintforge-complaint-handler
# Resolver Agent — Tasks

- [x] Implement `agents/resolver.py` and `ResolutionResult`.
- [ ] Add unit tests and validation for `refund_amount`/`credit_amount`.

Gaps / Recommended follow-ups:

- [ ] Add edge-case tests for escalation and confidence boundary behavior.
- [ ] Validate and normalize monetary fields before returning.
- [ ] Refactor to allow injection of a mock LLM for tests.

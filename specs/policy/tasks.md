# Policy — Tasks

- [x] Implement `nodes/policy.py` with deterministic rules.
- [ ] Add unit tests for all policy conditions and for `is_excluded_product` detection.

Gaps / Recommended follow-ups:

- [ ] Add tests for combined escalation reasons and policy output composition.
- [ ] Clarify the confidence reset behavior during escalation.
- [ ] Make policy thresholds configurable if needed.
